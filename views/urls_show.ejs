<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>

<body style="font-size:20px; padding:50px;">
  <header><% include ./partials/_header %></header>
  <!-- Check if user logged in, display page for shortURL link and form for upating this shortURL -->
  <%if (user){%>
    <h5>Short URL: <%=shortURL%></h5>
    <h6>This link was created on <%=createdTime%></h6>
    <h6>This link has been visited <%=counter%> times</h6>
    <h6>This link has been visited by <%=visitors%> different visitors</h6>
    
    <p>
      <% for(let entry of visits) {%>
       <li> <%=entry%> </li>
      <%}%>
    </p>

    <form class="form-inline" method="POST" action="/urls/<%=shortURL%>?_method=PUT">
    <div class="form-group mb-2">
      <label for="longURL">Enter the new URL:</label>
      <input class="form-control" type="text" name="newURL" placeholder="<%=longURL%>" style="width: 300px; margin: 1em">
      <button type="submit" class="btn btn-primary">Update</button>
    </div>
    </form>
  <%}else{%>
    <p>Please <a href = "/login">Login</a> or <a href = "/register">Register</a></p>
  <%}%>
</body>
</html>